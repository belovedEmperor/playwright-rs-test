[package]
name = "playwright-test"
version = "0.1.0"
edition = "2024"

[dependencies]
playwright-rs = { version = "0.9.0", default-features = false, features = ["rustls-tls-native-roots"] }
tokio = { version = "1", features = ["full"] }
